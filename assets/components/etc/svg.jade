mixin svg(svg, extraClass)
    - var classModificator = extraClass ? ' ' + extraClass : '';
    svg(focusable='false' version='1.1', class='svg-' + svg + classModificator)
        use(xlink:href='#svg-' + svg)
